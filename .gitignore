# Input/output data — keyword lists, crawl results, proxy lists



# ...except the dependency list, which belongs in the repo
!requirements.txt

__pycache__/

# Persistent browser profile and its pinned fingerprint — machine-local state,
# and the profile holds live Google cookies.
.browser_profile/
.fingerprint.json

# Artefak dari deploy/run-linktaker.sh
logs/
hasil/
.linktaker.lock
